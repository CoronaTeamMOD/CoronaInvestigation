import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    longAutoComplete: {
        width: 300
    },
    airportDetails: {
        display: 'flex',
        marginLeft: '1vw',
    },
    airportTextField: {
        width: '15vw',
        marginRight: '1vw',
    }
});

export default useStyles;