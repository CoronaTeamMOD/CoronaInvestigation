import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    title: {
        marginTop: '30px',
        fontSize: '1.15rem',
        fontWeight: 'bold',
    },
    field: {
        marginBottom: '8px',
    }
});

export default useStyles;