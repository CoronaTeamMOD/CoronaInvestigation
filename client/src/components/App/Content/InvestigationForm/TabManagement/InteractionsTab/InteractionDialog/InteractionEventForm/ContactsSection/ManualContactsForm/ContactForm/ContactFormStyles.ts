import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    additionalInfoItem: {
        paddingRight: '10rem'
    },
    moreContactDetails: {
        width: '40rem',
        margin: '1rem',
    },
    inputForm: {
        width: '16rem'
    }
});

export default useStyles;
