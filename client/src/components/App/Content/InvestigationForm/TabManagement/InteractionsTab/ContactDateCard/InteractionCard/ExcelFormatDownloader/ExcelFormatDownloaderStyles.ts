import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles({
    downloadButton: {
        display:'flex',
        alignItems: 'center',
        width: 'auto'
    },
    logo: {
        width: '3vw'
    },
    downloadText: {
        fontSize: '1rem'
    }
});

export default useStyle;