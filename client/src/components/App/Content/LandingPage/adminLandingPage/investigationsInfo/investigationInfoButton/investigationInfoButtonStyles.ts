import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    button: {
        color: 'white',
        borderRadius: '0.8vw',
        padding: '1vw 1vh',
        width: '100%',
        minHeight: '125px',
    },
    investigationAmount: {
        fontSize: '36px'
    }
});

export default useStyles;
