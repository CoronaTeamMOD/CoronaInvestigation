import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    button: {
        color: 'white',
        borderRadius: '0.8vw',
        padding: '1vw 1vh',
        margin: '0 1vw',
        flexBasis: 0,
        flexGrow: 1,
        height: '10vh'
    },
});

export default useStyles;
