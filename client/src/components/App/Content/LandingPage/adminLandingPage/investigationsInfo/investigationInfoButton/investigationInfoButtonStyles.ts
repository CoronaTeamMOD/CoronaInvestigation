import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    button: {
        color: 'white',
        borderRadius: '0.8vw',
        padding: '1vw 1vh',
        margin: '0 1vw',
        flexBasis: 0,
        flexGrow: 1,
        maxHeight: '10vh',
        minHeight: '100px',
    },
    investigationAmount: {
        fontSize: '36px'
    }
});

export default useStyles;
