import React from 'react';
import { Select, MenuItem } from '@material-ui/core';

const HospitalsInput: React.FC = () => {
    return (
        <Select>
            <MenuItem>sss</MenuItem>
            <MenuItem>sss</MenuItem>
            <MenuItem>sss</MenuItem>
        </Select>
    )
};

export default HospitalsInput;