import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles({
    button: {
        color: 'white',
        borderRadius: '10vw',
        height: '4vh',
        minWidth: '9vw',
    },
});

export default useStyles;
