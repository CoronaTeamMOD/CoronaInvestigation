import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
    selectInput: {
        borderRadius: 25,
        maxHeight: '4vh',
        margin: '0 2%',
        minWidth: '12vw',
    },
    resize: {
        fontSize: 13
    }
});